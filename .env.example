# Set this after deploying contracts/orbit-vault to Testnet.
# When unset, Orbit runs in demo mode (real Testnet payments + local share ledger).
VITE_ORBIT_VAULT_CONTRACT_ID=

# Admin Panel credentials
# Change these before deploying to any shared environment.
VITE_ADMIN_USER=
VITE_ADMIN_PASS=